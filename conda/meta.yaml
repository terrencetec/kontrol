package:
  name: "kontrol"
  version: "1.0.0"

about:
  summary: "Python package for KAGRA suspension and seismic isolation control."

source:
  path: ..

build:
  script: python setup.py install

requirements:
  build:
    - python

  run:
    - python
    - control >=0.9
    - numpy
    - matplotlib
    - scipy

